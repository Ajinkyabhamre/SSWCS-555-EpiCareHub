# This config was automatically generated from your source code
# Stacks detected: deps:node:Backend,package_manager:yarn:
version: 2.1
orbs:
  node: circleci/node@5
jobs:
  test-frontend:
    # Install node dependencies and run tests
    executor: node/default
    working_directory: ~/project/Frontend
    steps:
      - checkout:
          path: ~/project
      - node/install-packages:
          pkg-manager: yarn
      - run:
          name: Run tests
          command: yarn test --passWithNoTests
  test-backend:
   executor: node/default
   working_directory: ~/project/Frontend
   steps:
      - checkout:
          path: ~/project
      - node/install-packages:
          pkg-manager: yarn
      - run:
          name: Run tests
          command: yarn test --passWithNoTests
workflows:
  build-and-test:
    jobs:
      - test-frontend
      - test-backend
    # - deploy:
    #     requires:
    #       - test-node
